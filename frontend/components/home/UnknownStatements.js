import React from 'react'
import { View, Text } from 'react-native'

const UnknownStatements = ({ text }) => {
  return (
    <View>
      <Text>{text}</Text>
    </View>
  )
}

export default UnknownStatements
